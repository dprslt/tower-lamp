version: '3'

services:

  backend:
    build : ./backend
    ports :
      - "30008:30008"
    environment:
      FADE_CANDY_URL: "ws://192.168.1.71:7890"
    restart: always

  frontend:
    build: ./frontend
    ports:
    - 80:80
    restart: always
