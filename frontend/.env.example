# Backend Socket.IO server address (host:port, no scheme).
# Optional: when unset, the app derives it from the page's own hostname
# (same host as the UI, port 30008) — no hardcoded host/IP needed.
#   VITE_BACKEND_WS_URL=my-host:30008 npm run dev
#VITE_BACKEND_WS_URL=